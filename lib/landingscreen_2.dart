import 'package:flutter/material.dart';

class LandingScreen2  extends StatelessWidget {
  const LandingScreen2 ({super.key});

  @override
  Widget build(BuildContext context) {
    return const Image(
          image: AssetImage('image/LuckyJadePlant.png')
      );
  }
}