import 'package:flutter/material.dart';

class carouselImg3  extends StatelessWidget {
  const carouselImg3 ({super.key});

  @override
  Widget build(BuildContext context) {
    return const Image(
        image: AssetImage('image/Smallplant.png'),
    );
  }
}